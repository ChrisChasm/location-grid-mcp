# Docker-specific requirements for Location Grid MCP Server
# This file includes additional dependencies for containerized deployment

# Core MCP dependencies
mcp>=1.0.0

# Additional dependencies for Docker deployment
psutil>=5.9.0
watchdog>=3.0.0

# Optional: Add monitoring and logging
structlog>=23.0.0
